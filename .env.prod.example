# Django Configuration
DEBUG=0
SECRET_KEY=your-secret-key-here
DJANGO_ALLOWED_HOSTS=api.prayerpulse.io prayerpulse.io localhost 127.0.0.1

# Database Configuration
POSTGRES_USER=your-database-username
POSTGRES_PASSWORD=your-database-password
POSTGRES_DB=your-database-name

# Email Configuration (Optional - for user account features)
# If not using email features, leave these empty
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Social Auth Configuration (Optional - for OAuth login)
# If not using OAuth, leave these empty
SOCIAL_AUTH_GOOGLE_OAUTH2_KEY=
SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET=
SOCIAL_AUTH_GITHUB_KEY=
SOCIAL_AUTH_GITHUB_SECRET=

# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file: cp .env.prod.example .env.prod
# 2. Edit .env.prod with your actual credentials
# 3. Never commit .env.prod to git (it's in .gitignore)
